interface:
  display_name: "MKLink AI Probe"
  short_description: "Operate MKLink hardware, debug streams, serial, and GUI."
  default_prompt: "Use $mklink-ai-probe for this MKLink device task and load only the relevant reference."

policy:
  allow_implicit_invocation: true
