# 0.2.0 local reinstall and Skill consistency — 2026-09-07

## Scope and provenance

User authorized rebuilding the standard NSIS, overwriting the local installation,
synchronizing the public Skill, and opening Web GUI. Source: `6787302baa93ccf73a18078e13fe0fe96aa43f82`.
The same generated `gui/dist` was embedded into Tauri and inserted into the validated
public Skill archive. Both display `v0.2.0 · 6787302baa93`.
This follow-up commit stores generated assets and evidence only; it does not change runtime source.

## Verification

- `build.py --check` and `build.py --local-bundle` passed through the workspace wrapper.
- PyInstaller 6.21.0 / Python 3.14.5; Node 26.0.0; Rust 1.97.0.
- NSIS overwrite installation returned 0 with Windows system directories only in PATH.
  Desktop workspace hashes and shortcut were preserved.
- Installed sidecar SHA-256 matches the newly built sidecar. Desktop EXE is byte-identical
  except the expected three-byte Tauri bundle marker (`UNK` to `NSS`).
- Installed Tauri started with Windows-only PATH; process tree contains Tauri,
  standalone sidecar, conhost and WebView2, with no Python fallback.
- Real Tauri WebView: expected version, SuperWatch chip peripherals and SVD version
  history visible; health `ok`, backend port 8765, one probe enumerated; no page errors
  in the focused installation smoke test. No hardware writes or voltage changes.
- Normal window close released owned Mklink processes and TCP 8765.
- Extracted installed sidecar algorithm catalog and public Skill independently validate
  7,059 targets and 2,224 deduplicated FLM blobs.
- Public Skill: 2,448 managed files compared to the archive, all identical after update;
  obsolete GUI chunks removed; `.mklink` user configuration hashes unchanged; backup retained.
  No `AGENTS.md`, `CLAUDE.md`, `GEMINI.md`, `docs/ai`, `tests`, `_maintainer`, `skills`
  or `native` in the installation. Public instructions and on-demand user references remain.
- Installed Skill CLI, MCP import, 13 dependency imports, health and GUI index checks passed.
- Skill GUI files exactly match the desktop build input. Started Web GUI from installed
  Skill and verified the visible browser footer and peripheral selection controls.
  Device and serial remain disconnected for manual use.

## Limits and retained evidence

The older extended desktop navigation smoke script timed out while locating an online
flash button and, on another attempt, the offline model selector after navigation.
Direct online reload and the focused peripheral/version installation check passed;
this does not qualify the extended navigation scenario. Retain the failed logs for
separate investigation; no navigation code changed in this packaging task.
The initial history locator expected `CMSIS-SVD`, while actual release text says `SVD`;
the corrected check verified the actual user-facing peripheral description.

This local NSIS is unsigned (Windows may show unknown publisher), has no updater
signature, and is not a public release. No firmware qualification was repeated.
Full unchanged-source suites are recorded in the preceding PR #5 integration report.

Artifacts and SHA256SUMS: `.build/artifacts/v0.2.0-local-6787302-20260907/`.
NSIS SHA-256: `d078e9e11ae025b87a7ef9fa886e4aff01ec79e453b9c440e2330d90edcffc3c`.
Matched Skill ZIP SHA-256: `920becd8efc9e9a7e398b5fb92d7e2cf7aee487b2d3cc40de75e52ae1cd3dcc2`.
Raw install, consistency, Skill, browser and lifecycle evidence remains in
`.build/reports/local-install-20260907/` and workspace run logs; not distributed to users.
