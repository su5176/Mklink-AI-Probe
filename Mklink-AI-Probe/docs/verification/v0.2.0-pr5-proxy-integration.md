# PR #5：子路径代理接入 0.2.0

日期：2026-09-06。用户授权按审查建议修复并合入当前开发分支。

## 范围

- [PR #5](https://github.com/Aladdin-Wang/Mklink-AI-Probe/pull/5) 的 head 为
  `fb14a775878d12a8dd737bc00388124484af275a`，相对 `9222574` 新增 5 个提交。
  GitHub 目标已由 master 改为 `codex/v0.2.0-development`；保留 PR 提交历史。
- 接入 Vite 相对资源、HTTP/WS 子路径、浏览器会话及释放请求前缀、真实后端
  监听端口显示和表单 flex 收缩；不改变 SWD/烧录算法、GPIO 分图或采集缓冲。
- 保留本地 USB 暂缓跟踪记录；SuperWatch 外设中英文版本说明一并进入重建 GUI。
- 资源回归按 HTML/模块 URL 解析相对引用，继续检查懒加载脚本、Worker、CSS、
  文件存在和不可变缓存，未删除原检查。
- 发布给用户的 [代理配置](../../references/reverse-proxy.md) 明确入口 308
  重定向、剥离前缀、WebSocket Upgrade 与关闭 SSE 缓冲，并从 README 链接。
  不带斜杠入口由代理规范化；后端无法从剥离前缀后的请求恢复原始入口。
- 全量检查发现此前 SVD 解析绕过统一 pyOCD 导入锁，已使用
  `import_pyocd_module` 入口；解析规则不变。

## 实际代理与浏览器

使用官方 Nginx Windows 1.30.4、现有 Edge、独立 FastAPI 后端与临时空项目。
从用户文档直接提取 Nginx 示例，只替换临时监听端口；配置语法检查通过。
所有临时文件、二进制和日志保存在忽略的 `.build`，未安装系统服务。

- 根路径、带尾斜杠子路径、子路径 index.html 和无尾斜杠入口均加载成功。
- 无尾斜杠入口确实返回 308，浏览器跳转后保留 build 查询参数与
  `#/dashboard?tab=superwatch` fragment。
- GUI 显示独立后端的监听端口，未将代理公开端口显示为后端端口，也未改写
  传输地址绕过代理。
- 浏览器会话、SuperWatch 自动订阅、JSON-RPC 和 serial/RTT/SystemView
  WebSocket 均完成 101 握手；Nginx 日志确认关闭页面的带前缀释放请求返回 200。
- 版本弹窗确认中英文 SuperWatch 外设说明可见；程序变量/芯片外设目录保留。
- 无页面 JS 异常或资源错误；历史遗留 `/api/lang` 请求仍在根路径失败，单独
  记录且未把它计为本 PR 的新增资源错误。

未连接目标、打开串口、开始真机采集或刷固件；WS 握手不代表真机样本验证。
未重启用户原服务或修改其他部署平台。TLS、远程平台认证和安装包升级不属于
本轮本地代理验收。

## 回归与证据

第一次普通权限全量 Python 检查为 1880 通过、14 失败：12 项是 Windows
创建符号链接的权限不足；另外两项分别为既有 pyOCD 直接导入和新增文档的
用户包边界。后两项已修复，随后以 UAC 管理员测试进程复验完整套件。
测试未使用跳过来绕开门禁。最终 Python 完整套件为 **1896 通过、零失败、
零跳过**，GUI 完整套件为 **62 文件、667 通过**。版本说明测试原本固定断言
五条记录，已改为验证新增外设说明与手动分图内容，随后 GUI 全量复验通过。
TypeScript 检查和生产构建通过，最终构建仅更新来源提交标识并复验资源及浏览器。

证据目录：`.build/reports/pr5-merge`（Nginx 实际配置与访问日志、浏览器结果、
版本弹窗截图、完整 Python JUnit 结果）。各次构建及 GUI 测试输出保存于
`.build/reports/run-20260906-*`。含符号链接的测试临时目录由存储入口保留，
不强制递归清理。
